{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.post.ID}}">
			<section class="article-content">
				{{post.content}}
			</section>
		</article>
	</div>

{% endblock %}
